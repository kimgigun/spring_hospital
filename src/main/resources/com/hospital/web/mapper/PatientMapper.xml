<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.web.mapper.PatientMapper">
  <select id="selectById" resultType="com.hospital.web.domain.Patient" 
  parameterType="String">
    SELECT  
       pat_id AS patID,
       nur_id AS nurID,
       doc_id AS docID,
       pat_pass AS patPass,
       pat_name AS patName,
       pat_gen AS patGen,
       pat_jumin AS patJumin,
       pat_addr AS patAddr,
       pat_phone AS patPhone,
       pat_email AS patEmail,
       pat_job AS patJob
     FROM Patient WHERE pat_id = #{id}
  </select>
  
  <select id="count" resultType="int">
     SELECT COUNT(*) FROM Patient
  </select>
  
  <select id="exist" resultType="int" parameterType="String">
     SELECT COUNT(*) FROM Patient WHERE pat_id=#{id}
  </select>
  
  <select id="find" resultType="com.hospital.web.domain.Patient">
  
  SELECT
  	   pat_id AS patID,
       nur_id AS nurID,
       doc_id AS docID,
       pat_pass AS patPass,
       pat_name AS patName,
       pat_gen AS patGen,
       pat_jumin AS patJumin,
       pat_addr AS patAddr,
       pat_phone AS patPhone,
       pat_email AS patEmail,
       pat_job AS patJob
   FROM Patient WHERE state = ‘ACTIVE’ 
  <if test="title != null">
    AND title like #{title}
  </if>
</select>

<select id="find1"  resultType="com.hospital.web.domain.Patient">
  SELECT * FROM Patient WHERE state = ‘ACTIVE’ 
  <if test="title != null">
    AND title like #{title}
  </if>
  <if test="author != null and author.name != null">
    AND author_name like #{author.name}
  </if>
</select>

<select id="find2"  resultType="com.hospital.web.domain.Patient">
   SELECT
  	   pat_id AS patID,
       nur_id AS nurID,
       doc_id AS docID,
       pat_pass AS patPass,
       pat_name AS patName,
       pat_gen AS patGen,
       pat_jumin AS patJumin,
       pat_addr AS patAddr,
       pat_phone AS patPhone,
       pat_email AS patEmail,
       pat_job AS patJob
   FROM Patient WHERE state = ‘ACTIVE’  FROM Patient WHERE state = ‘ACTIVE’
  <choose>
    <when test="title != null">
      AND title like #{title}
    </when>
    <when test="author != null and author.name != null">
      AND author_name like #{author.name}
    </when>
    <otherwise>
      AND featured = 1
    </otherwise>
  </choose>
</select>

<select id="find3" 
     resultType="com.hospital.web.domain.Patient">
   SELECT
  	   pat_id AS patID,
       nur_id AS nurID,
       doc_id AS docID,
       pat_pass AS patPass,
       pat_name AS patName,
       pat_gen AS patGen,
       pat_jumin AS patJumin,
       pat_addr AS patAddr,
       pat_phone AS patPhone,
       pat_email AS patEmail,
       pat_job AS patJob
   FROM Patient WHERE state = ‘ACTIVE’  * FROM Patient 
  WHERE 
  <if test="state != null">
    state = #{state}
  </if> 
  <if test="title != null">
    AND title like #{title}
  </if>
  <if test="author != null and author.name != null">
    AND author_name like #{author.name}
  </if>
</select>


<select id="find4" resultType="com.hospital.web.domain.Patient">
   SELECT
  	   pat_id AS patID,
       nur_id AS nurID,
       doc_id AS docID,
       pat_pass AS patPass,
       pat_name AS patName,
       pat_gen AS patGen,
       pat_jumin AS patJumin,
       pat_addr AS patAddr,
       pat_phone AS patPhone,
       pat_email AS patEmail,
       pat_job AS patJob
   FROM Patient WHERE state = ‘ACTIVE’  * FROM Patient 
  <where> 
    <if test="state != null">
         state = #{state}
    </if> 
    <if test="title != null">
        AND title like #{title}
    </if>
    <if test="author != null and author.name != null">
        AND author_name like #{author.name}
    </if>
  </where>
</select>


<select id="selectPostIn" resultType="com.hospital.web.domain.Patient">
   SELECT
  	   pat_id AS patID,
       nur_id AS nurID,
       doc_id AS docID,
       pat_pass AS patPass,
       pat_name AS patName,
       pat_gen AS patGen,
       pat_jumin AS patJumin,
       pat_addr AS patAddr,
       pat_phone AS patPhone,
       pat_email AS patEmail,
       pat_job AS patJob
   FROM Patient WHERE state = ‘ACTIVE’ 
  FROM POST P
  WHERE ID in
  <foreach item="item" index="index" collection="list"
      open="(" separator="," close=")">
        #{item}
  </foreach>
</select>

<insert id="insert">
  <selectKey keyProperty="id" resultType="com.hospital.web.domain.Patient" order="BEFORE">
    <if test="_databaseId == 'oracle'">
      select seq_users.nextval from dual
    </if>
    <if test="_databaseId == 'db2'">
      select nextval for seq_users from sysibm.sysdummy1"
    </if>
  </selectKey>
   SELECT
  	   pat_id AS patID,
       nur_id AS nurID,
       doc_id AS docID,
       pat_pass AS patPass,
       pat_name AS patName,
       pat_gen AS patGen,
       pat_jumin AS patJumin,
       pat_addr AS patAddr,
       pat_phone AS patPhone,
       pat_email AS patEmail,
       pat_job AS patJob
   FROM Patient WHERE state = ‘ACTIVE’  users values (#{id}, #{name})
</insert>


</mapper>